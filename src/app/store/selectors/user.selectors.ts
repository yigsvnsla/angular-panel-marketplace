import { createSelector } from "@ngrx/store"
import { RootState } from "../states/root.state"

// const stateUser = (store:RootState)=>(store.stateUser)

// // export const selectorUser = createSelector(
// //   stateUser,
// //   (state)=> state.user
// // )

// export const selectorUserLoading = createSelector(
//  stateUser,
//  (state)=>state.loading
// )

// export const selectorUserToken = createSelector(
//   stateUser,
//   (state)=> state.token
// )

// export const selectorUserError = createSelector(
//   stateUser,
//   (state)=> state.error
// )
